<%= form_for(@channel) do |f| %>
  <% if @channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@channel.errors.count, "error") %> prohibited this channel from being saved:</h2>

      <ul>
      <% @channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, I18n.t('channel.form.label.name') %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :purpose, I18n.t('channel.form.label.purpose') %><br>
    <%= f.text_field :purpose %>
  </div>
  <div class="field">
    <%= f.select :member_ids, User.client.collect { |user| [ user.email, user.id ] }, {}, { multiple: true } %><br>
  </div>
  <div class="actions">
    <%= f.submit I18n.t('channel.button.create') %>
  </div>
<% end %>
